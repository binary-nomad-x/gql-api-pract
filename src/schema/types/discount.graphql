type Discount {
  id: ID!
  product: Product!
  name: String!
  type: DiscountType!
  value: Float!
  startDate: DateTime!
  endDate: DateTime!
  isActive: Boolean!
  maxUsage: Int!
  usedCount: Int!
  createdAt: DateTime!
  updatedAt: DateTime!
}
