type Subscription {
  id: ID!
  user: User!
  plan: SubscriptionPlan!
  status: SubscriptionStatus!
  startDate: DateTime!
  endDate: DateTime
  autoRenew: Boolean!
  cancelledAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}
